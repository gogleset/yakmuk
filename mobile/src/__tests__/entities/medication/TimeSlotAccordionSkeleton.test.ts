/** Reanimated Skeleton 의존 — 모듈 경로 계약만 */
describe('TimeSlotAccordionSkeleton', () => {
  it('경로 계약', () => {
    expect('@/entities/medication/ui/TimeSlotAccordionSkeleton').toContain(
      'TimeSlotAccordionSkeleton',
    );
  });
});
