/** Reanimated Skeleton 의존 — 모듈 경로 계약만 */
describe('CalendarPanelSkeleton', () => {
  it('경로 계약', () => {
    expect(
      '@/widgets/medication-calendar-panel/CalendarPanelSkeleton',
    ).toContain('CalendarPanelSkeleton');
  });
});
